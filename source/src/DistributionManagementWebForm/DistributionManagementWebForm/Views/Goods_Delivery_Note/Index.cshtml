@model IEnumerable<DistributionManagementWebForm.Models.Goods_Delivery_Note>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.delivery_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.delivery_method)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order_Note.payment_method)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.delivery_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.delivery_method)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order_Note.payment_method)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.delivery_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.delivery_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.delivery_id })
        </td>
    </tr>
}

</table>
