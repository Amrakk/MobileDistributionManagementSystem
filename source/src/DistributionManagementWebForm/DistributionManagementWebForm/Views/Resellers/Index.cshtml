@model IEnumerable<DistributionManagementWebForm.Models.Reseller>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.reseller_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Profile.first_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.reseller_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Profile.first_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.reseller_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.reseller_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.reseller_id })
        </td>
    </tr>
}

</table>
